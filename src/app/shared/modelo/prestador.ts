export class Prestador {
    id: string;
    nomeFantasia: string;
    especialidade: string;
  
    constructor(id: string, nomeFantasia: string, especialidade: string) {
      this.id = id;
      this.nomeFantasia = nomeFantasia;
      this.especialidade = especialidade;
    }
  
  }
  